apiVersion: v1
kind: Service
metadata:
  name: {{ include "database-dev.fullname" . }}
  labels:
    {{- include "database-dev.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "database-dev.selectorLabels" . | nindent 4 }}
