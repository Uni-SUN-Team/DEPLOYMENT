apiVersion: v1
kind: Secret
metadata:
  labels:
    {{- include "kubernetes-dashboard.labels" . | nindent 4 }}
    k8s-app: {{ .Chart.Name }}
  name: {{ .Chart.Name }}-certs
type: Opaque

---

apiVersion: v1
kind: Secret
metadata:
  labels:
    {{- include "kubernetes-dashboard.labels" . | nindent 4 }}
    k8s-app: {{ .Chart.Name }}
  name: {{ .Chart.Name }}-csrf
type: Opaque
data:
  csrf: {{ .Values.secret.csrf | b64enc | quote }}

---

apiVersion: v1
kind: Secret
metadata:
  labels:
    {{- include "kubernetes-dashboard.labels" . | nindent 4 }}
    k8s-app: {{ .Chart.Name }}
  name: {{ .Chart.Name }}-key-holder
type: Opaque