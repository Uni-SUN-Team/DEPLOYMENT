apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "database-dev.fullname" . }}-test-connection"
  labels:
    {{- include "database-dev.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "database-dev.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
